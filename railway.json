{
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/ping",   // ← यहाँ /ping है, /api/ping नहीं
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
